#version 140

uniform vec4 color;
uniform sampler2D tex_0;

in vec2 tex_0c;
out vec4 frag_color;

void main()
{
	vec4 image = texture2D(tex_0, tex_0c);
	frag_color = vec4(color.r, color.g, color.b, color.a*image.r);
}
